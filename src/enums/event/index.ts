export enum EventType {
  REGULAR = 'regular',
  SPECIAL = 'special',
}

export enum FrequencyType {
  ONCE = 'once',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
  CUSTOM = 'custom',
}
